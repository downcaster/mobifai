# Mobile App Configuration
# ⚠️ Note: React Native doesn't read .env files without additional libraries
# Update src/config.ts directly instead

# Your Mac's local IP address (find in System Settings → Network)
MAC_IP=192.168.1.102

# Relay server URL
# - For iOS Simulator: use http://localhost:3000 (shares host network)
# - For Physical Device: use http://YOUR_MAC_IP:3000
RELAY_SERVER_URL=http://192.168.1.102:3000

# Enable debug mode
DEBUG_MODE=true
